version: '3.9'

services:
  queue-storm-investigator:
    build: .
    ports:
      - '8080:8080'
    env_file:
      - .env
    restart: unless-stopped
    command: uvicorn queue_storm.app:app --host 0.0.0.0 --port 8080
